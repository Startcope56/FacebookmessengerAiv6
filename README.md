🚀MOR FACEBOOK MESSAGER V5 

🛡️ Safety Features:

✅ Each clone has its own admin set (creator + their adminUID + main admins)
✅ /create blocked from running inside cloned bots (no recursion)
✅ Per-clone appstate auto-save (60s interval)
✅ Auto-detects logout/cookie expiry → marks clone as logged_out
✅ Survives bot restart — restoreAll() on startup
✅ Clone uses high-speed mobile UA (anti-detect)
✅ Each clone runs selfListen: false (won't reply to itself)
